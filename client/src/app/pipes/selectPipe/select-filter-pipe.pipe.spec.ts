import { SelectFilterPipePipe } from './select-filter-pipe.pipe';

describe('SelectFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
